[project]
name = "delta-computer"
authors = ["Delta Computer Team"]
description = "Computational asset tokenization platform"
telemetry = false
requirements = []
[contracts.delta-registry]
path = "contracts/delta-registry.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
